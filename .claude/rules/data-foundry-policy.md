---
paths:
  - "foundry/**/*"
  - "docs/data-foundry.md"
  - "scripts/foundry/**/*"
  - "scripts/validate-data.ts"
---
<!-- Action card, loaded when foundry files are opened. NOT a policy restatement. -->
<!-- Derived, non-authoritative (decision (114)): the policy itself lives in §7.1/§8/§12/§15 + the
     vault decision log; this card has no standing of its own. Change trigger: edit only when the
     stop-set or gate axis changes. Codified through vault decision (124) — a later decision must
     update this card or record an explicit no-op; the session-end ritual bumps the cursor. -->
<!-- Authority: Obsidian vault decision log (final) → docs/data-foundry.md §7.1/§8/§15 (in-repo). -->
<!-- If this card and those sources diverge, the card is wrong — fix the card, log the divergence. -->

## Foundry action card

- **Read first (session start):** vault `Noosphere/index.md` decision log + `roadmap.md` — do not
  reconstruct state from memory. `npm run foundry:recheck-held` for the held/anchors worklist.
- **Generation/QC split (ADR 0007, §15.7):** separated-context generation is required exactly when
  the promotion gate includes an LLM verdict; fully mechanical gates may be orchestrator-drafted.
  Model routing = vault decisions (76)/(99)③ — do not assume, check the log.
- **Decision autonomy (decision (91), §7.1):** the gate axis is "does this change
  policy/identity/schema or trip a narrow risk signal," NOT "is this a decision." Proceed and log
  on: wave scoping among sanctioned tracks; applying ratified ladders/policies; mirror rulings;
  post-measurement mirror-ladder openings (notify, CPO veto window); mechanical QC outcomes;
  housekeeping. **Stop only for:** a new risk-family policy/ladder; a schema/taxonomy change; a
  strategy pivot / phase transition; living-person narrow escalation signals (70); a boundary the
  rules are genuinely silent on; live evidence refuting a ratified premise; reversing a ratified
  policy. Over-gating routine/mirror decisions is the twin of defeatism ((89)/(70) discipline).
- **Ladders are code:** `foundry:ladder-check` is the arithmetic; `foundry:apply-batch` is the ONLY
  write path to /data. Divergence between ratified policy text and code is itself a stop-point.
- **Full policy lives in docs/data-foundry.md:** promotion ladders + keep-criteria (§8), evidence
  permanence (§8), clause-6 v2 (§8), living-person (70) (§8), taxonomy-authority v1 (§8),
  editorial v2 QC regime (34) (§8), hold-resolution standard (§12), toolchain (§15). Standing
  sanctions outside ladders (e.g. B-track external-metrics, decision (27)) = vault decision log.
