# Batch report — `structural-nodes-v1`

**Round 4 · session #40 · 2026-06-30.** Surfaced structural-node closure from the person waves
(decisions (67)/(71)). Scope set by the orchestrator: the two **clean** surfaced nodes only —
`subfield:structural-anthropology` and `person:roman-jakobson` — with the movement-level pair
(`vienna-circle`, `logical-positivism`) deliberately held out of scope for a CPO movement-axis ruling.

## Outcome
- **1 node + 2 edges → `reviewed`** (Jakobson + his phonology-founding and Lévi-Strauss-influence edges).
- **1 node + 2 edges → `proposed`** (structural-anthropology + its founder edge + the diffuse
  semiotics edge), routed to the CPO movement-axis ruling / re-examination.
- Generator QID hints **2/2 hallucinated**, both caught and corrected by live multi-signal QC.
- Nodes 472→474 · edges 559→563 · translations 472→474 · sources 21. typecheck ✓ validate ✓.

## Headline judgments
1. **Jakobson is a clean person-contract-v1 admission** — principal co-founder of structural phonology
   (with Trubetzkoy), the historical mediation link Saussure→Jakobson→Lévi-Strauss. Auto-`reviewed`.
2. **Structural anthropology is NOT a clean subfield** — live QC confirmed the generator's `ambiguous`
   flag: it is a Lévi-Strauss-centric *school* of sociocultural anthropology, which is the parked
   movement-axis question, not an auto-buildable peer subfield. Held at `proposed`; the orchestrator
   did not unilaterally resolve a parked design question.
3. **The ladders behaved correctly** — supported edges between reviewed endpoints auto-promoted; the
   founder edge into the proposed target was correctly held; the diffuse edge stayed proposed.

## Parallel work (separate track)
The Lane A practical/meta scoping brief (idle since 2026-06-19) was completed in parallel: both
`domain:practical-knowledge` and `domain:meta-knowledge` → **(a) layer/axis** (broad overlap;
meta-knowledge with a CPO scope caveat). Research-only; CPO input. See
`reference/lane-A-practical-meta-scoping-brief.md` in the vault.

## Artifacts
`proposals.json` (untrusted generated draft) · `translations.proposed.json` · `qc-report.md` ·
`promotion-report.md` · `report.md`.
