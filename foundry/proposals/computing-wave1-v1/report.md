# Report — `computing-wave1-v1`

> Generator context returned this report as final-message text (the harness Write guard blocks
> subagent report files); the orchestrator wrote it to disk verbatim. Provenance below is the
> generator's own self-report.

**Batch:** `computing-wave1-v1`, session #64, 2026-08-08, round 4 coverage axis — executes decision (121)'s corrected unblock for `person:john-mauchly` / `person:j-presper-eckert` in `foundry/held.json`. Generation only; this context does not QC its own output. **[NO-EXTERNAL-EVIDENCE]** at this generation step — no network access, every `source_hint` is an unverified pointer for a separate QC context to fetch/verify per `docs/data-foundry.md` §8. Model: Claude Sonnet 5, exact ID `claude-sonnet-5`.

**Pre-flight check (done before proposing):** confirmed via `Grep` of `data/nodes.json`/`data/edges.json` that none of `person:john-mauchly`, `person:j-presper-eckert`, `person:john-atanasoff`, or any ENIAC/EDVAC artifact node exist canonically; confirmed `person:john-von-neumann`, `subfield:computer-systems`, `concept:internet` are `reviewed`; confirmed both anchor precedents (`edge:john-von-neumann-influenced-computer-systems` 0.85, `edge:vint-cerf-founded-internet` 0.9) exist as stated. Read `foundry/held.json` in full for the three relevant ledger rows before drafting.

**Counts:** 4 nodes proposed / 0 reconciled to existing / 1 node flagged ambiguous (`concept:stored-program-computer`). 7 edges proposed / 0 reconciled / 7 flagged ambiguous. Zero outright refusals — every slate row was honestly proposable at some (often low) confidence with reasoning exposed, per ADR 0007's instruction to expose uncertainty rather than convert it into silence or false confidence.

**Item 3 referent decision (argued, not asserted):** chose `concept:stored-program-computer` over the machine referents (closed by decision (121)) and over "von Neumann architecture" specifically — that name credits one author and this corpus's own reviewed edge note already calls it controversial for excluding Mauchly/Eckert, so routing their founder credit *into* that same name would be an avoidable irony. The neutral concept name is shared by all three proposed founder edges (Mauchly, Eckert, von Neumann), mirroring the `concept:internet` precedent. Flagged `ambiguous: true`: real risk of resolver collision with a "von Neumann architecture" entity or no clean match at all (the same C3 failure mode already on record for `concept:boolean-algebra`/`concept:cardinality`).

**Lane A vs. Lane B:** both proposed as two candidates, not pre-resolved, per the order naming both shapes. Lane A (`influenced` → `subfield:computer-systems`) at confidence 0.8 (above wave-12's 0.75, on the strength of the order's additional named sources), explicitly noting wave-12's split-verification outcome (Mauchly rejected / Eckert supported on identical evidence, judged a search-path artifact) as a real possible recurrence, not something this proposal assumes away. Lane B (`founded_or_formalized` → `concept:stored-program-computer`) at confidence 0.55, honestly below the corpus's normal founder-edge range because only one claim-stating source is known to me (the "one source short" diagnosis already in `foundry/held.json`) — offered as a testable candidate, with NEI recorded as a legitimate possible QC outcome.

**Item 5 (von Neumann → concept):** confidence 0.85, parity with the existing reviewed edge, since it rests on the same already-accepted source family for a stronger, more precisely targeted claim. Its note explicitly records the credit-sharing tension (record-not-resolve) rather than resolving it.

**Item 4 (Atanasoff) — schema note QC must see:** `disputed` is not a field on `proposedEdgeV2Schema` (`src/schema/foundry-proposal.ts`, `.strict()`, fields are `id, source, target, relation, confidence, status, evidence_kind, evidence, source_hint, note, rationale, uncertainty, ambiguous` only). Both Atanasoff edges record **"SHOULD CARRY disputed:true AT PROMOTION"** in the `note` field instead of as a JSON key, since adding an unrecognized key would fail schema validation. Counter-evidence is recorded in the note per the order's explicit instruction, honestly marked as unverified/from-memory rather than fabricated as a live quote. Both edges are stated as conditional on `person:john-mauchly`/`person:j-presper-eckert` landing first.

**Deliberately left out:** no `founded_or_formalized` for Atanasoff himself (artifact-grained, no in-scope concept referent); no work/`canonical_work` nodes for ENIAC/EDVAC (decision (121) already closed that path); no direct Mauchly↔Eckert edge (not in the order's five items).

**What QC should look at first:** (1) live-resolve `concept:stored-program-computer`'s referent before anything downstream, since 3 of 7 edges target it; (2) re-run the Lane A search for both men independently rather than trusting this proposal, since a repeat split is a legitimate outcome; (3) locate and quote the actual Atanasoff counter-evidence sentence rather than relying on this proposal's paraphrase; (4) decide, as a judgment this generator deliberately did not pre-empt, whether Lane A and Lane B should co-exist if both clear their bars.
