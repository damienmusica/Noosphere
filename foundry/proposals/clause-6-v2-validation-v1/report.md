# Clause-6 v2 Validation (§3.1 no-node re-adjudication) — batch report

**Session #35, 2026-06-29.** Design gate precursor: `clause-6-disputed-redesign-design.md` (vault),
CPO-ratified Part 1 (5 points). This batch = Part 2 (§3.1): re-adjudicate three prior-NEI cases
under clause-6 v2 **using only existing nodes**, to measure whether v2 *fires* (disputed-balanced)
and whether it *over-fires*.

## Pipeline
Sonnet generation (separate context) assembled candidate per-camp claim-stating sources →
orchestrator (Opus) independently re-grounded every cited source live + adversarial QC. Generation
draft untrusted, as always: its C3 Camp-A SEP anchor was a **hallucination** (caught), exactly the
fallible-generator / independent-grounding-absorbs pattern from #34.

## Result — `disputed:true` fired **0/3** (cumulative **0/32**)
- **C3 pragmatism→analytic** — generation said FIRE; **QC overturned → does NOT fire.** The
  existence of cross-influence is *largely agreed* (NDPR reviewer: the independence/"isolationist"
  position is a straw man "wholly or largely uncontested"); IEP's "separate tradition" is
  classification, not denial. → supported+note territory, not disputed. Not written (out of scope).
- **C2 buddhist→phenomenology** — **NEI holds.** Affirm camp empty (parallel/comparative; direction
  actually phenomenology→Kyoto School). Over-fire guard holds trivially.
- **C1 darwin→mendel** — **does NOT fire disputed.** Verified ≥2 live PMC sources that Darwin
  influenced Mendel's *1866-paper framing* (not his experiments — agreed independent) → degree/
  character, not existence. Prior NEI now outdated → supported+note territory. Not written.

## Reading
- **v2's guards are validated** — the existence-vs-degree line + per-camp source floor blocked all
  three, and adversarial QC actively **reversed** a draft over-fire (C3). No manufactured disputes.
- **v2's positive fire is undemonstrated** — none of the no-node NEI cases is a genuine balanced
  existence-split. The textbook balanced-splits (Nietzsche→Freud) need new contested-influence nodes
  (design §3.2), deferred at ratification ⑤. v2 *cannot* fire on existing nodes.
- **Codification does NOT execute** (per ratified discipline ③: codify only after a correct fire).
  v2 stays ratified-but-unproven; `docs/data-foundry.md` §8 unchanged this session.

## /data impact — none
No nodes, edges, sources, or translations changed. typecheck ✓ validate ✓. The two supported+note
re-adjudication candidates (C1, C3) are recommended to a follow-up (a)-wave, not written here.

Files: `scoping.md` + `proposals.json` (generation draft, untrusted) · `qc-report.md` (orchestrator
verdicts) · this report.
