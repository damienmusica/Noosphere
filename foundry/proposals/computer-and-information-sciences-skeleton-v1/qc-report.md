# QC report — computer-and-information-sciences-skeleton-v1

- **QC by:** Claude Fable 5 (orchestrator session #8), 2026-06-11.
- **Generated by:** Claude Sonnet (claude-sonnet-4-6) subagent, separate context (ADR 0007 generation/QC separation upheld).
- **Input:** 28 generated nodes (1 field + 27 subfields), 0 edges.
- **Output:** 27 nodes (25 kept, 2 QC gap-fill additions), 0 edges. 3 dropped, 6 A-type flags retired, 2 B-type flags retained.

## Generation-side fetch failure and QC re-grounding

The generator's live-fetch mandate was blocked at every gate source (loc.gov 403, dl.acm.org 403,
web.archive.org unreachable from the subagent, udcsummary.info navigation-only) — it self-marked
**all 28 nodes [UNFETCHED]** and fell back to arXiv cs.* (live), FORD top-level (live), and
Wikipedia (live). The contract held: no training knowledge was laundered into confident prose.
Consequence: **every classification claim was re-grounded live by QC**, with these access paths
(recorded per the bot-block protocol):

| Source | QC access path | Result |
|---|---|---|
| LCC Q outline (QA75.5–76.95) | local curl of lcco_q.pdf (WebFetch 403) | live, full |
| LCC Z outline (Z665–718.8) | local curl of lcco_z.pdf | live, full |
| LCC cutter-level records | id.loc.gov `/authorities/classification/<num>.json` | live, 15+ records |
| UDC 004 (57 records) + 02 (11 records) | udcsummary.info `php/getrecord.php?id=<class>&lang=1` | live, full captions |
| ACM CCS 2012 (full tree) | Wayback snapshot 20191108093737 of `dl.acm.org/ccs/ccs_flat.cfm` (live site Cloudflare-blocked) | full top-2 levels + spot paths |
| MSC 2020 section 68 | msc2020.org `MSC_2020.csv` (direct, live) | full |
| OECD FORD | arrs.si Frascati mirror (registered source) | 1.2 / 5.8 confirmed |
| arXiv cs.* | generator-fetched (live), auxiliary only | 41 categories |

Generator hint corrections found during re-grounding: `QA76.9.A25` is not the algorithms cutter
(QA76.9.A43 is, verified); `QA268`'s own caption is "Algebra--Machine theory--Coding theory", not
"cryptography"; LCC files machine learning at **Q325.5 under Cybernetics**, not as a QA peer of CS.

## QC criteria and continent-specific interpretations (recorded as precedent)

1. **Dual criterion** per §12, with two interpretations this continent forces:
   - **ACM CCS 2012 is a gate-level classification source here** (the batch manifest lists it in
     the coverage baseline; MSC 68 and FORD 1.2 are the cross-checks). LCC compresses the whole
     computing continent into ~1.5 class numbers, so UDC/LCC alone cannot carry criterion (a).
   - **LCC cutter-level subdivisions inside QA75.5–76.95 count as major divisions** — cutters are
     how LCC subdivides this continent.
2. Refinement absorption, cross-continent two-prong rule, and label rule per §12 unchanged.
3. Identity judgments: QIDs remain unverified hints until the resolver pass (decision log (9)).

## Structural decisions

| Decision | Verdict | Reason |
|---|---|---|
| `field:machine-learning` level-1 legacy (pre-registered) | **Demote to `subfield:machine-learning` (level 2) at promotion, with canonical ID migration** | mathematical-logic precedent applies cleanly: UDC files ML under 004.8 Artificial intelligence (live); CCS files AI and ML as sibling second-level branches of Computing methodologies (live); LCC files ML at Q325.5 under Cybernetics (live). Nothing supports institutional field-level standing — ML is groups-within-CS-departments. Migration touches 10 references across 5 data files (nodes, edges, translations, external-links, learning-paths); the reviewed edge `machine-learning-part-of-computer-science` keeps its semantics (source ID updated). AI and ML enter as **peer subfields** (CCS sibling structure; UDC's ML-under-AI noted, not enforced as an edge). |
| `subfield:artificial-intelligence` inversion flag | **Kept level-2; flag retired (A)** | Resolved by the ML demotion above. |
| `subfield:theoretical-computer-science` umbrella | **Kept alongside finer peers; flag retired (A)** | §12 peer-coexistence rule (metaphysics/ontology; mathematical-logic + ASL pillars): CCS top level "Theory of computation" + MSC 68Qxx + SIGACT/"theory group" department naming give the umbrella its own standing; algorithms, complexity, and formal-languages each carry distinct communities. |
| `subfield:cryptography` vs `subfield:computer-security` | **Both kept; flag retired (A)** | CCS separates "Cryptography" from the rest of "Security and privacy" (live); MSC separates 94A60/68P25 from 68M25 (live); IACR vs IEEE S&P/CCS-conference communities are distinct. Executes the ratified §12 transfer (cryptography→CS). |
| `subfield:computer-systems` umbrella | **Kept; flag retired (A)** | CCS top level "Computer systems organization" + UDC 004.2/004.45 + MSC 68Mxx (all live); OS/architecture/performance absorbed as v2 re-split candidates; hardware-proper stays excluded (engineering). |
| `field:library-and-information-science` level + wing scope (pre-registered) | **Kept level-1 field; flag retired (A); no policy escalation** | LCC grants the wing its own range Z665–718.8 and UDC its own class 02 (both live); iSchools/LIS faculties are department-level units of their own (unlike ML); FORD 1.2's own title names the wing. §12 two-prong resolves it — **the potential CPO gate (a) did not trigger**. |
| `subfield:computer-graphics-and-visualization` | **Dropped (malformed scope); QC gap-fill `subfield:visualization`** | The generated node overlapped the kept `subfield:computer-graphics`. CCS separates "Visualization" (Human-centered computing) from "Computer graphics" (Computing methodologies) — both live; LCC QA76.9.I52 "Information visualization" (live); IEEE VIS/TVCG vs SIGGRAPH communities. |
| `subfield:social-computing` cross-continent pull | **Kept; flag retired (A)** | CCS "Collaborative and social computing" second level (live, gate source); CSCW/ICWSM community sits in CS/iSchools. Computational social science is a distinct social-sciences referent → deliberate non-coverage. |
| `subfield:scientific-computing` boundary | **Kept; B-type flag retained → stops at proposed** | Real-world boundary contest with applied mathematics/numerical analysis (SIAM CSE spans both). UDC 004.94 + CCS Applied computing branch + MSC 68U20/68Vxx (live) anchor the keep; the contest awaits a clause-6 pass. |
| `subfield:quantum-computing` boundary | **Kept; B-type flag retained → stops at proposed** | LCC QA76.889 "Quantum computers" (live) files it **inside** the continent range → stays per §12 rule 3 first prong; MSC dual-files 68Q12/81P68; physics institutional claim is real → clause-6 candidate. |

## Dropped nodes (3)

| Dropped | Disposition | Reason |
|---|---|---|
| subfield:bioinformatics | **Deferred to life_sciences / medicine_and_health** | §12 two-prong: LCC home QH324.2 "Biology (General)--…--Data processing. Bioinformatics" (id.loc.gov, live) is outside the continent; dominant institutional home is bioinformatics programs/institutes in life-science and medical faculties (ISCB ecosystem). Mirrors biostatistics→medicine. |
| subfield:archival-science | **Dropped — criterion (a) fails in-continent (novel ruling)** | Classification homes are LCC CD (Diplomatics. Archives. Seals) and UDC 930.25 — both in another continent's classes (history/humanities) — while the institutional home (iSchools) is inside this one. Novel case: **a department-named area whose classification home lies in another continent waits for that continent's skeleton**; §13 cross-listing can connect it to LIS later with evidence. Recorded as deliberate non-coverage, not silence. |
| subfield:computer-graphics-and-visualization | **Dropped (malformed scope)** | Replaced by QC gap-fill `subfield:visualization` (see structural decisions). |

## QC gap-fill additions (2)

| Added | Why the generator's coverage missed it |
|---|---|
| subfield:visualization | Generator folded it into a malformed combined node; CCS separates the branch (live). IEEE VIS/TVCG community. No QID hint — resolver must establish one. |
| subfield:history-of-computing | Generator missed the CCS "History of computing" branch (Social and professional topics, live) and LCC QA76.17 (live, inside continent range). history-of-mathematics precedent symmetry; IEEE Annals + SIGCIS. No QID hint — resolver must establish one. |

## Deliberate non-coverage (recorded so silence is not ambiguity)

- **Hardware / computer engineering** (CCS Hardware top level; UDC 004.2/004.3 architecture+hardware boards; LCC TK): engineering continent (manifest exclusion). UDC 004.2/004.31-38 hardware-proper claims recorded; the systems-software side is kept inside `subfield:computer-systems`.
- **Robotics** (arXiv cs.RO; LCC TJ211): both prongs outside → engineering continent.
- **cs.SY systems and control**: `subfield:control-theory` canonical in formal sciences (+ its engineering membership edge, PR #47).
- **Media and communications** (FORD 5.8): social sciences (manifest exclusion).
- **Computers and society / Social and professional topics (CCS)**, **computing education / CS education research** (CCS "Computing education", live): social-sciences/education boundary; no UDC/LCC division of their own in this continent. SIGCSE/ICER noted for a future pass.
- **Computational social science**: social sciences; distinct from kept social-computing.
- **Business-school information systems** (AIS ecosystem): social sciences (management).
- **Data science**: named departments/institutes exist (criterion (b) passes) but **no classification division of its own** in UDC 004 / LCC / CCS 2012 / MSC 68 (all live) — criterion (a) fails; representation-theory pattern. First-in-line v2 candidate when classifications catch up.
- **Web science / World Wide Web** (CCS "World Wide Web" second level, live): WebSci/TheWebConf community exists, but department-level standing folds into IR/networks/social-computing; absorbed (v2 re-split candidate).
- **Ubiquitous and mobile computing** (CCS second level, live): UbiComp community absorbed under HCI umbrella (v2 candidate).
- **Computational geometry** (arXiv cs.CG; MSC 68U05 shared caption, live): SoCG community absorbed into algorithms-and-data-structures (v2 candidate).
- **Multimedia, sound/audio computing (cs.MM, cs.SD)**: level-3 granularity.
- **Operating systems, computer architecture, performance (cs.OS, cs.AR, cs.PF)**: absorbed into computer-systems (v2 re-split candidates).
- **Symbolic computation / mathematical software (cs.SC, cs.MS)**: absorbed into scientific-computing.
- **Document processing (UDC 004.91, CCS "Document management and text processing", live)**: absorbed into NLP/IR.
- **Logic in CS (cs.LO; CCS "Theory of computation > Logic", live)**: LICS community absorbed into theoretical-computer-science; the discipline-level referents are canonical in formal sciences (mathematical-logic + pillars).
- **Information theory, game theory, computability theory**: canonical in formal sciences; CS-side second memberships are session task 3 (§13), not skeleton re-proposals (generator complied).
- **Archival science**: see drop table (novel ruling).

## Coverage cross-check (QC sweep result)

Every ACM CCS 2012 top-level branch, every UDC 004/02 summary division (57+11 records, live), every
LCC QA75.5–76.95 and Z665–718.8 outline division, and every MSC 68 subsection (68M/N/P/Q/R/T/U/V/W,
live CSV) now maps to exactly one kept node, one documented absorption, or one documented
exclusion/deferral above — no unaddressed gaps. The arXiv cs.* table (41 categories) in the
generator's report.md is consistent with this sweep after the three drops and two gap-fills.

## What the resolver pass should watch

1. `subfield:visualization` and `subfield:history-of-computing` — **no QID hints** (QC gap-fills);
   resolver must establish or they stay in foundry (v1.2 alternative grounding otherwise).
2. `subfield:computer-systems` hint Q7946 — generator itself labeled it "computer architecture";
   likely wrong referent for a systems umbrella.
3. `subfield:theoretical-computer-science` hint Q466 — short-QID hallucination risk (Q466 is
   unlikely to be TCS).
4. `field:library-and-information-science` hint Q13420675 — LIS vs "library science" vs
   "information science" entity split on Wikidata; sitelink+alias discipline required.
5. `subfield:databases-and-information-systems` hint Q8513 ("database" — an artifact, not a
   discipline) and `subfield:algorithms-and-data-structures` hint Q464118 — probable
   field-vs-object mismatches; resolver type-fit reranking should handle.
6. All remaining hints are unverified training knowledge (~27% accuracy measured in the
   formal-sciences batch); the resolver verdict, not the hint, is the evidence.
