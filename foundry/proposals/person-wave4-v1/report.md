# person-wave4-v1 — batch report

**Session #39, round 4 Lane B, decision (70), 2026-06-30.** Person wave 4 = **living-person handling
v2 first execution**. The first time the risk-tiered v2 standing policy (ratified #38) is *run* — the
headline is that a clean, uncontested *living* founder is written `reviewed` **by policy** (no per-item
CPO sign-off), generalizing the Seligman N=1 precedent to N>1.

## What this batch is
- **Bucket A (4 deceased surfaced co-founders):** Marx→sociology, Koch→microbiology, Peirce→semiotics,
  Wallace→evolutionary-biology — each a co-founder *alongside* a founder already in /data (record-not-
  resolve plural founding; decision (62) residual debt). Auto-promote `reviewed` on the founder ladder.
- **Bucket B (2 scoped-living founders — the v2 measurement):** Labov→sociolinguistics,
  Lakoff→cognitive-linguistics.
- **Bucket C (Cerf→`concept:internet`, CPO opt-in):** closes the #33 honest gap by pointing at the
  precise referent (the Internet, Q75) instead of the over-broad `computer-networks` field.

## Outcome
- Generator QID hallucination **6/8 ≈ 75%** (only Marx + Internet correct), **all caught & corrected**
  via full multi-signal live resolver verification (P31=Q5 + P569 + P570 + label/sitelink).
- Edge precision **7/7 = 1.0**; claim-level hallucination **0/8**; **7 supported / 0 disputed / 0 NEI /
  0 reject**. 6 record-not-resolve plural-founding sets preserved.
- **★ Observe-only self-correction fired:** **Labov, scoped as living (b.1927), was live-confirmed
  deceased (P570 = 2024-12-17)** → recorded `is_living_person:false`, routed to the deceased founder
  ladder. Drift ran living→deceased (stricter→looser) — the safe direction v2 §3 predicts. Living N = 2.
- **★ v2 policy-auto confirmed (the headline):** **Lakoff and Cerf written `reviewed` by policy** — they
  met the v2 floor (resolver-verified QID anchor + P570 live-confirmed absent + ≥2 independent live
  claim-stating sources + conservative attributed wording) with **no escalation signal**, so **no CPO
  per-item sign-off**. **Living N: 1 → 3.** v2 is demonstrably *not* a blanket gate.
- **Escalations fired: 0** (the narrow signal set — disputed/NEI/reject, thin sourcing, private/
  reputational content, subject dispute — stayed empty).
- **`concept:internet`** admitted as a QC modeling ruling (concept type already in schema; founder
  taxonomy admits field/concept targets) — not a schema change.
- **Written to /data:** **8 nodes** (7 person + 1 concept) `reviewed` + **7 `founded_or_formalized`
  edges** `reviewed` (5 deceased via founder ladder + 2 living via v2 policy-auto). Schema/taxonomy
  unchanged; no new sources. typecheck + validate green.

## Node/edge deltas
- Nodes 464 → **472** (+7 person, +1 concept; cumulative person 36 → 43, of which **2 living** — Lakoff,
  Cerf; Seligman remains the prior living node, so living total = 3).
- Edges 552 → **559** (founded_or_formalized +7).
- Translations 464 → **472**. Sources 21 (unchanged).

See `qc-report.md` for the full QID-correction table, per-edge grounding, the Labov observe-only finding,
the Lakoff/Wallace adversarial reversals, and the Cerf/internet modeling ruling. `qc-resolver-
groundtruth.md` for the raw live-resolver record. `scoping.md` for the ratified Stage 0 design.
`promotion-report.md` for what entered /data and under which policy.
