# a-relations-wave4-v1 — report

**Phase-2 cross-cutting intellectual-edge densification, wave 4 (Stage-3 axis ②), session #51,
2026-07-02.** Continues the `a-relations-wave3-v1` densification of the part_of-dominated graph with
`influenced`/`critiques` edges between already-`reviewed` nodes.

Generation: separated-context **Claude Sonnet 5** (`proposal-generator`, ADR 0007) — 16 real
candidates + 2 unmarked rejection probes. QC: orchestrator live-verification (this session), Lane B
pipeline per docs/data-foundry.md §8. Permanence anchors were taken **at QC time** by the
orchestrator (not the generation step): Wikipedia native oldid revision permalinks + SEP/MacTutor
Wayback Save-Page-Now snapshots. Evidence URLs in `proposals.json` were unverified generator hints;
every promoted edge was re-grounded against ≥2 independent live claim-stating sources before promotion.

## Outcome

- **11 supported → auto-`reviewed`** via the (68) (a)-relation ladder (both endpoints already
  `reviewed`, supported verdict, no disputed/NEI/reject, no living-person escalation).
- **5 held `proposed`** — 1 endpoint status-cap (`computer-systems` still `proposed`), 1 living-person
  + modeling-precision re-model (`chomsky`), 3 diffuse/thin.
- **2/2 reject probes fired** (`darwin→law`, `cantor→psychoanalysis`).
- **Claim-level hallucination: 0.** Two generator citation slips caught and substituted at QC.

Full per-candidate verdicts and anchors: `qc-report.md`. What entered `/data`: `promotion-report.md`.
Reject-probe key (opened post-verdict): `probes.md`.

## Coverage notes

- `subfield:philosophy-of-race` deliberately not touched — no sourceable person/work endpoint exists
  for it yet; not forced (a-fortiori honest-gap discipline).
- `subfield:philosophy-of-cognitive-science` (newly `reviewed` under node-policy v1.4 this session)
  used as an endpoint via a well-attested Husserl→embodied-cognition lineage (B6), the intended
  low-risk first use of a v1.4-promoted node.
- Canonical early-modern philosophers (Kant, Hegel, Descartes, Locke, Hume, Spinoza) have no person
  nodes in the corpus — flagged by the generator as a future node-proposal gap, not an (a)-edge choice
  for this wave.
- Relation mix: `influenced` 15, `critiques` 2 among the 16 real candidates (1 `critiques` promoted,
  1 held).

Evidence permanence for this batch is recorded in `qc-report.md` and `promotion-report.md` (oldid
permalinks + SPN snapshots); no `[NO-EXTERNAL-EVIDENCE]` marker is used (this batch's verdicts rely on
external pages, all anchored).
